# Project Conventions

> Auto-generated by `project-dna` skill from SPEC.md and ARCHITECTURE.md.
> Enforces project-specific coding patterns and conventions.

---

## File Naming

| Type | Convention | Example |
|------|-----------|---------|
| Components | [e.g., PascalCase] | `UserProfile.tsx` |
| Services | [e.g., kebab-case] | `user.service.ts` |
| Tests | [e.g., same as source + .test] | `user.service.test.ts` |
| Types | [e.g., kebab-case] | `user.types.ts` |
| Constants | [e.g., kebab-case] | `api.constants.ts` |
| Utilities | [e.g., kebab-case] | `date.utils.ts` |

## Code Organization

### Import Order

```typescript
// 1. External dependencies (node_modules)
import { Injectable } from '@nestjs/common';

// 2. Shared/cross-cutting modules
import { Logger } from '@/shared/logger';

// 3. Same bounded context imports
import { User } from '../domain/user.entity';
import { UserRepository } from '../domain/user.repository';
```

### File Structure Pattern

```typescript
// 1. Imports (grouped as above)
// 2. Types/Interfaces (if small; otherwise separate file)
// 3. Constants (if any)
// 4. Main export (class, function, component)
// 5. Helper functions (private, not exported)
```

## Error Handling

- **Pattern:** [e.g., Result<T, E> / Exceptions / Either]
- **Domain errors:** [e.g., Custom error classes extending DomainError]
- **Application errors:** [e.g., Map domain errors to application-level responses]
- **Infrastructure errors:** [e.g., Catch and wrap in application-level errors]
- **Never:** Swallow errors silently. Never use empty catch blocks.

```typescript
// Example error handling pattern
// [Fill with project-specific pattern]
```

## API Design Conventions

- **Endpoint naming:** [e.g., RESTful: plural nouns, kebab-case]
- **Response format:** [e.g., { data: T, meta?: object, error?: object }]
- **Status codes:** [e.g., 200 success, 201 created, 400 validation, 404 not found, 500 internal]
- **Validation:** [e.g., Validate at controller level, domain validates business rules]

## Git Conventions

- **Branch naming:** `feat/`, `fix/`, `refactor/`, `docs/`, `test/` prefixes
- **Commit format:** Conventional commits with scope matching bounded context
- **PR size:** Prefer small, focused PRs (1 feature or fix per PR)

---

*Source: SPEC.md code style (Stage 4), ARCHITECTURE.md patterns (Stage 5)*
*Generated: YYYY-MM-DD*
